# Decision Log

Total decisions: 000

Append-only audit log. Each entry has an anchor for precise retrieval.
`CLAUDE.md` instructs Claude to read only the last 5 entries (or last 3 for a specific domain tag).

---

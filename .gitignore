# Credentials (never commit)
.env

# Runtime state (project-specific, not for public repo)
state/
__pycache__/

# System files
.DS_Store
*.pyc

# Sandboxed test environment (gitignored, not for distribution)
sandbox/
